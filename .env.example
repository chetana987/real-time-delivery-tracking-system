# Copy this file to .env and adjust the values.
# Never commit the real .env file; keep it out of version control.

# ---------------- Backend ----------------
# REQUIRED. JWT signing secret (HS256, must be at least 32 bytes).
# The backend refuses to start unless this is set. Generate one with:
#   openssl rand -hex 32
# Replace this placeholder with your own random value.
JWT_SECRET=replace-with-a-random-development-secret
JWT_EXPIRATION_MS=86400000
PARTNER_SEARCH_RADIUS_KM=10
LOCATION_UPDATE_MIN_INTERVAL_SECONDS=2

# ---------------- MySQL ----------------
MYSQL_DB=delivery_tracking
MYSQL_USER=delivery
MYSQL_PASSWORD=change-me
MYSQL_ROOT_PASSWORD=change-me-root

# ---------------- Redis ----------------
REDIS_PASSWORD=change-me
